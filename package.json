{
  "name": "wordarium-hugo-theme",
  "version": "1.0.0",
  "description": "Custom Hugo Theme based on bootstrap",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile:sass": "node-sass -w ./assets/sass/style.scss ./assets/css/style.css"
  },
  "author": "Seann Hicks",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^7.0.0"
  }
}
